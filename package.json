{
  "name": "react-complete-guide",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.11.6",
    "@testing-library/react": "^11.2.2",
    "@testing-library/user-event": "^12.5.0",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "react-scripts": "4.0.1",
    "web-vitals": "^0.2.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "docker-build": "docker build --tag gubarew/react .",
    "docker-run": "docker run -d -p 3000:3000 -v react:/effects-reducers-context/data --rm --name reactapp gubarew/react",
    "docker-run-dev": "docker run -d -p 3000:3000 -v C:/Itransition/Projects/Udemy/Современный React с нуля (2022)/Effects_Reducers_Context/effects-reducers-context:/app -v /app/node_modules -v react:/app/data --rm --name reactapp gubarew/react",
    "docker-start": "docker start reactapp",
    "docker-stop": "docker stop reactapp"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
